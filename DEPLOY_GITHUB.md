# Walton SOP Maker Enterprise - GitHub & Vercel Upload Guide

এই ফোল্ডারের ফাইলগুলো সরাসরি GitHub-এ আপলোড করে Vercel-এ লাইভ করার জন্য সম্পূর্ণ প্রস্তুত।

---

## 📁 ফোল্ডারের ধরন:
* **`walton-sop-maker`** : আপনার লোকাল ডেভেলপমেন্ট প্রজেক্ট (node_modules সহ)।
* **`github-upload-walton-sop-maker`** : গিটহাব আপলোডের জন্য সম্পূর্ণ ক্লিন ও লাইটওয়েট ফাইল (কোনো ভারী node_modules বা dist নেই)।

---

## 🚀 GitHub-এ আপলোড করার ধাপসমূহ:

1. আপনার ব্রাউজারে GitHub রিপোজিটরি ওপেন করুন:  
   👉 **https://github.com/nipuruet10-creator/walton-sop-maker**

2. রিপোজিটরির ভেতরে গিয়ে **Add file** বাটনে ক্লিক করে **Upload files** সিলেক্ট করুন।

3. আপনার কম্পিউটারের এই ফোল্ডারটি ওপেন করুন:  
   `E:\Antigravity\Process Automation Projects\github-upload-walton-sop-maker`

4. এই ফোল্ডারের ভেতরের সমস্ত ফাইল ও ফোল্ডার (`src`, `public`, `package.json`, `index.html`, `vite.config.ts`, ইত্যাদি) সিলেক্ট করে ব্রাউজারের ড্র্যাগ অ্যান্ড ড্রপ বক্সে ছেড়ে দিন।

5. নিচে স্ক্রোল করে **Commit changes** বাটনে ক্লিক করুন।

---

## ⚡ নতুন যুক্ত হওয়া এন্টারপ্রাইজ ফিচারসমূহ:

### ১. ছবিতে এডিটিং ও ড্রয়িং টুল (Photo Annotator Canvas):
* যেকোনো ছবিতে লাল রঙের বক্স (Rectangle), তীরচিহ্ন (Arrow), বৃত্ত (Circle), মার্কার পেন (Freehand), এবং টেক্সট লেবেল সরাসরি এঁকে হাই-রেজ্যুলিউশনে সেভ করার সুবিধা।

### ২. ক্লিপবোর্ড পেস্ট (`Ctrl+V`) ও ড্র্যাগ-অ্যান্ড-ড্রপ:
* স্নিপিং টুল বা ব্রাউজার থেকে ছবি কপি করে সরাসরি অ্যাপে `Ctrl+V` চাপলেই স্বয়ংক্রিয়ভাবে SOP ফটো হিসেবে যুক্ত হবে।

### ৩. লক্ষণীয় বিষয় (Critical Quality Points) ফন্ট সাইজ:
* ইনপুট প্যানেল ও ক্যানভাস ফ্লোটিং বার থেকে ফন্ট সাইজ অ্যাডজাস্ট করার অপশন (`Auto`, `ছোট`, `স্বাভাবিক`, `বড়`)।

### ৪. ইউজার অথেনটিকেশন ও অ্যাক্সেস রোল:
* **Prepared By (প্রস্তুতকারী):** `Biplob`, `Dev` (পাসওয়ার্ড: `Process@2026`)
* **Checked By (উচ্চপদস্থ পর্যালোচক):** `Sazzad`, `Rafi`, `Hashmi`, `Pear`, `Emon` (পাসওয়ার্ড: `Process@2026`)
* **Approved By (অনুমোদনকারী):** `Kamrul` (পাসওয়ার্ড: `Process@2026`)
* **System Admin (পাসওয়ার্ড রিকভারি ও কন্ট্রোল):** ID: `Sazzad`, Pass: `ACprocess@2026`

### ৫. ৩-স্তর বিশিষ্ট অনুমোদন ও ফরওয়ার্ডিং ওয়ার্কফ্লো:
* প্রস্তুতকারী স্বাক্ষর আপলোড করে উচ্চপদস্থ পর্যালোচকের কাছে ফরওয়ার্ড করবেন ➔ পর্যালোচক স্বাক্ষর যোগ করে Kamrul Hasan এর কাছে পাঠাবেন ➔ কামরুল হাসান চূড়ান্ত অনুমোদন করবেন।

### ৬. কনসার্ন সেকশন ও পাবলিক সেন্ট্রাল ড্যাশবোর্ড:
* অনুমোদিত SOP-গুলো সরাসরি কনসার্ন সেকশন ড্যাশবোর্ডে চলে আসবে, যেখান থেকে যেকোনো অপারেটর বা ইঞ্জিনিয়ার ১-ক্লিকে অনুমোদিত PDF বা Excel ডাউনলোড করতে পারবেন।

### ৭. অ্যানালিটিক্স ড্যাশবোর্ড (ইউজারভিত্তিক ও মাসভিত্তিক):
* কার নামে কয়টি SOP সম্পন্ন হয়েছে তার তালিকা এবং মাসভিত্তিক (Monthwise) ও বছরভিত্তিক (Yearwise) বার চার্ট গ্রাফ।

### ৮. অ্যাডমিন প্যানেল ও পাসওয়ার্ড রিকভারি:
* অ্যাডমিন যেকোনো ইউজারের পাসওয়ার্ড রিসেট করতে পারবেন এবং সম্পূর্ণ ডাটাবেজ মাস্টার ব্যাকআপ (JSON) নিতে ও রিস্টোর করতে পারবেন।
